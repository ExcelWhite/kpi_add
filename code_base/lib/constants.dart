import 'dart:ui';

import 'package:flutter/material.dart';

class AppColors {
  final Color backgroundColor = Color(0xff2c2c2c);
  final Color primaryColor = Colors.amber;
  final Color secondaryColor = Colors.deepOrange;
  final Color textColor = Colors.white;
  final Color textSecondaryColor = Colors.black;
  final Color cardColor = Color.fromARGB(255, 233, 231, 203);
  final Color approvedColor = Colors.lightGreen;
}